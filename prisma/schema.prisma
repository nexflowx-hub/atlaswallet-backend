generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// Financial domain models are introduced in Stage 03.
// The schema is intentionally minimal in the bootstrap stage.
